% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sutra_spatial.R
\name{sutra_semivariogram}
\alias{sutra_semivariogram}
\title{Empirical semivariogram of model residuals}
\usage{
sutra_semivariogram(d, response, col, row, fit = NULL, maxlag = 12)
}
\arguments{
\item{d}{data.frame of plot records.}

\item{response, col, row}{column names.}

\item{fit}{a fitted SpATS object (optional).}

\item{maxlag}{largest lag distance, in plots.}
}
\value{
data.frame with columns \code{lag}, \code{gamma_raw}, \code{gamma_adj}.
}
\description{
Reports residual spatial autocorrelation before and after the fitted spatial
surface is removed. A post-correction curve that is flat to within sampling
error is the diagnostic that Module 1 worked; a curve that still rises means
spatial structure is being left in the residual.
}
\examples{
sim <- sutra_simulate(n_gen = 20, n_env = 1, n_rep = 2, seed = 1)
s <- sutra_spatial(sim$data, response = "y", genotype = "genotype",
                   col = "col", row = "row")
head(s$diagnostics$semivariogram)
}
